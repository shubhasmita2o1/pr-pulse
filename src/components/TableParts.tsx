import { ArrowUpDown, ChevronLeft, ChevronRight } from 'lucide-react'
import { Button } from './ui'
import type { ReactNode } from 'react'
export function TableWrap({heads,children}:{heads:string[];children:ReactNode}){return <div className="overflow-hidden rounded-xl border border-border bg-card shadow-card"><div className="max-h-[620px] overflow-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="sticky top-0 z-10 bg-sand"><tr>{heads.map((h,i)=><th key={h} className="px-5 py-3 text-[11px] font-bold uppercase text-muted-foreground">{h}{i<5&&<ArrowUpDown className="ml-1 inline size-3"/>}</th>)}</tr></thead><tbody className="divide-y divide-border">{children}</tbody></table></div></div>}
export function Pagination({count}:{count:number}){return <div className="mt-4 flex items-center justify-between text-xs text-muted-foreground"><span>Showing {count} records</span><div className="flex items-center gap-1"><Button variant="secondary" size="icon" aria-label="Previous page"><ChevronLeft className="size-4"/></Button><span className="px-3 font-semibold text-foreground">1 of 1</span><Button variant="secondary" size="icon" aria-label="Next page"><ChevronRight className="size-4"/></Button></div></div>}
export const tone=(v:string)=>v==='Active'||v==='Ready'||v==='Live'||v==='Complete'||v==='Completed'||v==='Connected'?'success':v==='Pending'||v==='Prospect'||v==='Processing'||v==='Partial'?'warning':v==='Churned'||v==='Failed'?'danger':v==='Draft'?'coral':'neutral' as const
