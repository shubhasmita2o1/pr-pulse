import { createFileRoute,Link } from '@tanstack/react-router';import { Search,AudioLines,Clock,Users,ExternalLink } from 'lucide-react';import { transcripts } from '@/data/mock';import { Badge,Card,Input,PageHeader } from '@/components/ui';
export const Route=createFileRoute('/transcripts')({head:()=>({meta:[{title:'Transcripts — PR Client & Campaign Tracker'},{name:'description',content:'Review client call transcripts and action items.'},{property:'og:title',content:'Call Transcripts'},{property:'og:description',content:'Review agency call transcripts and next steps.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Transcripts})
function Transcripts(){return <><PageHeader title="Transcripts" subtitle="Review conversations, decisions, and follow-up items."/><div className="relative mb-4 max-w-sm"><Search className="absolute left-3 top-3 size-4 text-muted-foreground"/><Input placeholder="Search transcripts…" className="pl-9"/></div><div className="grid gap-4">{transcripts.map(t=><Link key={t.id} to="/transcripts/$transcriptId" params={{transcriptId:t.id}}><Card className="flex items-center gap-5 p-5 transition hover:border-primary/40 hover:shadow-md"><div className="grid size-11 place-items-center rounded-lg bg-primary-soft text-primary"><AudioLines className="size-5"/></div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h2 className="font-bold">{t.title}</h2><Badge tone={t.source==='Read AI'?'teal':'neutral'}>{t.source}</Badge></div><p className="mt-1 text-xs text-muted-foreground">{t.client} · {t.date}</p></div><div className="hidden items-center gap-5 text-xs text-muted-foreground md:flex"><span className="flex gap-1"><Clock className="size-3.5"/>{t.duration}</span><span className="flex gap-1"><Users className="size-3.5"/>{t.speakers} speakers</span><span>{t.meeting}</span><ExternalLink className="size-4"/></div></Card></Link>)}</div></>}
